# OS
.DS_Store

# Node / TS (if the TUI ends up JS-based)
node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.local

# Python (kokoro engine / if the TUI ends up Python-based)
.venv/
__pycache__/
*.pyc
.uv/

# Rust / Go (if applicable)
target/

# Runtime artifacts
*.mp3
*.wav
readaloud*.tmp/

# User config (live; committed template is config.example.toml)
/config.toml

# Python packaging
*.egg-info/
